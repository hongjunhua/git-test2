git指令：

	一.查看指令：
		git log  ：查看详细提交日记
		git log --oneline   : 简略查看提交日记



	二.提交
		git add 提交到暂存库s
		git add . 把当前所以文件提交到暂存库
		git commit -m (文件名) 把文件从




		git checkout -- index.html  把这个修改过的文件返回到上一次修改
		git checkout -- .  把所有修改过的文件返回